defaults:
  - train_cli
  - _self_

db:
  url:
    drivername: "sqlite"
    database: ":memory:"

model:
  steps:
    features:
      transformers:
        example:
          transformer:
            with_std: true
