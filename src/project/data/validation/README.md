# Data Validation

This subpackage contains data models to validate the data.

## Raw Model

The raw data is validated on its data types.

## ML-Conform Model

The processed data is validated on ML-conform data types.
