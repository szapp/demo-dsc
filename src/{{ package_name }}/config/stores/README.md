# Hydra Config Stores

The subpacke contain the config stores for Hydra. They correspond to exchangeable groups of modulare configurations.
They can be imported collectively with `from .config import store`.

The Hydra config is loaded automatically. The other config groups are selected via Hydra-defaults.
