# @package _global_
defaults: [/.exp_multi]

# Experiment metadata
experiment:
  name: multi-feature-check-example_${now:%Y%m%d}_${now:%H%M%S}}
  description: |
    Compare feature toggle combinations.

# Experiment configuration
hydra:
  sweeper:
    params:
      model.steps.feature_selection.transformers.passthrough.columns.col1: true,false
      model.steps.feature_selection.transformers.passthrough.columns.col2: true,false
